import {describe,it,expect} from "vitest"; import {detectRecurring} from "@/domain/detection"; import {normaliseMerchant,transactionFingerprint} from "@/domain/transactions";
describe("transactions",()=>{it("normalises aliases",()=>expect(normaliseMerchant("CARD MICROSOFT*365 AU")).toBe("MICROSOFT 365"));it("creates stable fingerprints",()=>{const x={date:"2026-01-01",description:"Microsoft*365",amount:-100,account:"Card"};expect(transactionFingerprint(x)).toBe(transactionFingerprint(x))})});describe("recurrence",()=>{it("detects monthly payments with evidence",()=>{const rows=["2026-01-01","2026-02-01","2026-03-01","2026-04-01"].map(date=>({date,description:"MICROSOFT*365",amount:-100}));const [c]=detectRecurring(rows);expect(c.cycle).toBe("MONTHLY");expect(c.transactions).toHaveLength(4);expect(c.score).toBeGreaterThan(.8)});it("ignores one-offs",()=>expect(detectRecurring([{date:"2026-01-01",description:"Officeworks",amount:-100}])).toEqual([]))})
