import {Shell} from "@/components/shell";
import {requireAdvisor} from "@/lib/session";
import {listClients,listSubscriptions} from "@/lib/repository";
import {createSubscription} from "@/app/actions";
export const dynamic="force-dynamic";
const money=(n:number)=>new Intl.NumberFormat("en-AU",{style:"currency",currency:"AUD"}).format(n);
export default async function Page({searchParams}:{searchParams:Promise<{client?:string}>}){const session=await requireAdvisor(),clients=await listClients(session),subscriptions=await listSubscriptions(session),selected=(await searchParams).client||clients[0]?.id||"";return <Shell active="Subscriptions"><div className="page-head"><div><div className="eyebrow">Portfolio register</div><h1>Subscriptions</h1><p>Annual costs follow each client&apos;s GST-inclusive or GST-exclusive presentation setting.</p></div></div><section className="card"><details open={!subscriptions.length}><summary>Add subscription manually</summary>{clients.length?<form action={createSubscription} className="form-grid compact"><label><span>Client</span><select name="clientId" defaultValue={selected}>{clients.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label><span>Provider</span><input name="provider" required/></label><label><span>Category</span><input name="category" defaultValue="Software" required/></label><label><span>Billing cycle</span><select name="billingCycle"><option>MONTHLY</option><option>QUARTERLY</option><option>ANNUAL</option><option>WEEKLY</option></select></label><label><span>Amount per cycle (AUD)</span><input name="amount" type="number" min="0" step="0.01" required/></label><label><span>GST treatment</span><select name="gstTreatment"><option value="UNKNOWN">Unknown / review required</option><option value="GST_INCLUSIVE">GST inclusive</option><option value="GST_EXCLUSIVE">GST exclusive</option><option value="GST_FREE">GST free</option></select></label><label><span>Owner</span><input name="ownerName"/></label><label><span>Renewal date</span><input name="renewalDate" type="date"/></label><label><span>Cancellation notice (days)</span><input name="noticeDays" type="number" min="0"/></label><button className="btn primary">Add subscription</button></form>:<div className="empty-state">Add a client first.</div>}</details></section><section className="card top-gap">{subscriptions.length?<table className="table"><thead><tr><th>Client</th><th>Provider</th><th>Billing</th><th>Annual cost</th><th>GST treatment</th><th>Owner</th><th>Renewal</th><th>Status</th></tr></thead><tbody>{subscriptions.map(s=><tr key={s.id}><td>{s.clientName}</td><td className="client-name">{s.provider}<div className="muted">{s.category}</div></td><td>{s.billingCycle}</td><td className="amount">{money(s.annualCost)}</td><td>{s.gstTreatment.replaceAll("_"," ")}</td><td>{s.ownerName||"Unassigned"}</td><td>{s.renewalDate||"Not recorded"}</td><td><span className="pill">{s.status}</span></td></tr>)}</tbody></table>:<div className="empty-state">No subscriptions confirmed yet.</div>}</section></Shell>}
