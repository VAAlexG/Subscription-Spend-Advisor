import {Shell} from "@/components/shell"; import {recommendations,money} from "@/lib/demo-data";
export default function Page(){return <Shell active="Recommendations"><div className="page-head"><div><div className="eyebrow">Advisor control</div><h1>Recommendation queue</h1><p>Review evidence and wording before anything becomes client-visible.</p></div><button className="btn dark">Generate drafts</button></div><div className="notice" style={{marginBottom:18}}><b>Publication rule:</b> recommendations require evidence, retrieval date, assumptions, confidence and explicit advisor approval.</div><div className="section-stack">{recommendations.map(r=><section className="card" key={r.title}><div className="card-head"><div><span className={`pill ${r.state==="Draft"?"draft":r.state==="Needs approval"?"review":""}`}>{r.state}</span><h3 style={{margin:"10px 0 3px",color:"var(--ink)"}}>{r.title}</h3><div className="card-sub">{r.provider} · {r.type}</div></div><div className="metric-value">{money(r.saving)}</div></div><p style={{fontSize:13}}>{r.reason}</p><div className="client-row" style={{gridTemplateColumns:"1fr 1fr 1fr auto"}}><div><div className="muted">Confidence</div><b>{r.confidence}</b></div><div><div className="muted">Evidence</div><b>2 current sources</b></div><div><div className="muted">Migration effort</div><b>Medium</b></div><div className="actions"><button className="btn">Edit</button><button className="btn primary">Approve</button></div></div></section>)}</div></Shell>}
