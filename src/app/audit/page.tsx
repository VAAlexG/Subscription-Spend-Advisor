import {Shell} from "@/components/shell";import {requireAdvisor} from "@/lib/session";import {listAudit} from "@/lib/repository";export const dynamic="force-dynamic";
export default async function Page(){const events=await listAudit(await requireAdvisor());return <Shell active="Audit log"><div className="page-head"><div><div className="eyebrow">Governance</div><h1>Audit log</h1><p>Traceable advisor and automated-system actions.</p></div></div><section className="card">{events.length?<table className="table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Client</th><th>Summary</th></tr></thead><tbody>{events.map(e=><tr key={String(e.id)}><td>{new Date(String(e.created_at)+"Z").toLocaleString("en-AU")}</td><td>{String(e.actor_email||"System")}</td><td className="client-name">{String(e.action)}</td><td>{String(e.client_name||"Firm")}</td><td><code>{JSON.stringify(JSON.parse(String(e.summary_json)))}</code></td></tr>)}</tbody></table>:<div className="empty-state">Actions will appear here as the pilot workflow is used.</div>}</section></Shell>}
