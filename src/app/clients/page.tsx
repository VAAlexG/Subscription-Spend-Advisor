import Link from "next/link"; import {Shell} from "@/components/shell"; import {clients,money} from "@/lib/demo-data";
export default function Page(){return <Shell active="Clients"><div className="page-head"><div><div className="eyebrow">Portfolio</div><h1>Clients</h1><p>Separate workspaces, review status and savings performance.</p></div><Link className="btn primary" href="/clients/new">Add client</Link></div><section className="card"><table className="table"><thead><tr><th>Client</th><th>Annual spend</th><th>Subscriptions</th><th>Potential savings</th><th>Progress</th><th>Status</th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td><Link className="client-name" href={`/clients/${c.id}`}>{c.name}</Link><div className="muted">{c.industry}</div></td><td className="amount">{money(c.spend)}</td><td>{c.subscriptions}</td><td className="amount">{money(c.savings)}</td><td><div className="progress"><span style={{width:`${c.progress}%`}}/></div></td><td><span className={`pill ${c.status==="In review"?"review":c.status==="Draft"?"draft":""}`}>{c.status}</span></td></tr>)}</tbody></table></section></Shell>}
