import Link from "next/link";import {Shell} from "@/components/shell";import {requireAdvisor} from "@/lib/session";import {listClients} from "@/lib/repository";export const dynamic="force-dynamic";const money=(n:number)=>new Intl.NumberFormat("en-AU",{style:"currency",currency:"AUD",maximumFractionDigits:0}).format(n);export default async function Page(){const clients=await listClients(await requireAdvisor());return <Shell active="Clients"><div className="page-head"><div><div className="eyebrow">Portfolio</div><h1>Clients</h1><p>Separate workspaces, review status and savings performance.</p></div><Link className="btn primary" href="/clients/new">Add client</Link></div><section className="card">{clients.length?<table className="table"><thead><tr><th>Client</th><th>Annual spend</th><th>Subscriptions</th><th>Potential savings</th><th>Progress</th><th>Status</th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td><Link className="client-name" href={`/clients/${c.id}`}>{c.name}</Link><div className="muted">{c.abn?`ABN ${c.abn}`:"ABN not recorded"}</div></td><td className="amount">{money(c.spend)}</td><td>{c.subscriptions}</td><td className="amount">{money(c.savings)}</td><td><div className="progress"><span style={{width:`${c.progress}%`}}/></div></td><td><span className="pill review">{c.status}</span></td></tr>)}</tbody></table>:<div className="empty-state">No clients yet.</div>}</section></Shell>}
