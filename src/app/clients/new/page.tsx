import Link from "next/link";import {Shell} from "@/components/shell";import {createClient} from "@/app/actions";import {requireAdvisor} from "@/lib/session";export const dynamic="force-dynamic";export default async function Page(){await requireAdvisor();return <Shell active="Clients"><div className="page-head"><div><div className="eyebrow">New workspace</div><h1>Add a client</h1><p>Create a tenant-isolated workspace for a subscription review.</p></div></div><section className="card form-card"><form action={createClient} className="form-grid"><label><span>Business name</span><input name="name" required minLength={2}/></label><label><span>ABN</span><input name="abn" inputMode="numeric"/></label><label><span>Financial presentation</span><select name="gstMode"><option value="INCLUSIVE">GST inclusive</option><option value="EXCLUSIVE">GST exclusive</option></select></label><div className="actions"><Link className="btn" href="/clients">Cancel</Link><button className="btn primary" type="submit">Create workspace</button></div></form></section></Shell>}
