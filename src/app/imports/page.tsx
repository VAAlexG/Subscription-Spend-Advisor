import {Shell} from "@/components/shell";import {requireAdvisor} from "@/lib/session";import {listClients,listImports,listCandidates} from "@/lib/repository";import {importTransactions} from "@/app/actions";export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{client?:string}>}){const session=await requireAdvisor(),clients=await listClients(session),imports=await listImports(session),candidates=await listCandidates(session),selected=(await searchParams).client||clients[0]?.id||"";return <Shell active="Imports"><div className="page-head"><div><div className="eyebrow">Transaction intake</div><h1>Import Xero CSV</h1><p>Upload, map, validate and analyse transactions for recurring payments.</p></div></div><div className="grid"><section className="card"><div className="card-title">Import transactions</div>{clients.length?<form action={importTransactions} className="form-grid"><label className="wide"><span>Client workspace</span><select name="clientId" defaultValue={selected} required>{clients.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label className="wide file-field"><span>CSV export (maximum 20 MB)</span><input name="file" type="file" accept=".csv,text/csv" required/></label><div className="wide card-sub">Column names can be changed to match the exported file.</div><label><span>Date column</span><input name="dateColumn" defaultValue="Date" required/></label><label><span>Description column</span><input name="descriptionColumn" defaultValue="Description" required/></label><label><span>Amount column</span><input name="amountColumn" defaultValue="Amount" required/></label><label><span>Account column</span><input name="accountColumn" defaultValue="Account"/></label><label><span>Reference column</span><input name="referenceColumn" defaultValue="Reference"/></label><button className="btn primary" type="submit">Import and detect</button></form>:<div className="empty-state">Add a client before importing transactions.</div>}</section><aside className="card"><div className="card-title">Import safeguards</div><div className="renewal"><div><h4>Duplicate protection</h4><p>Stable transaction fingerprints prevent repeat imports.</p></div></div><div className="renewal"><div><h4>Source retained</h4><p>The original CSV is retained in protected object storage.</p></div></div><div className="renewal"><div><h4>Evidence exposed</h4><p>{candidates.filter(c=>c.status==="PENDING").length} candidates currently await advisor review.</p></div></div></aside></div><section className="card top-gap"><div className="card-title">Import history</div>{imports.length?<table className="table"><thead><tr><th>Client</th><th>File</th><th>Accepted</th><th>Rejected</th><th>Imported</th></tr></thead><tbody>{imports.map(i=><tr key={String(i.id)}><td className="client-name">{String(i.client_name)}</td><td>{String(i.filename)}</td><td>{String(i.imported_rows)}</td><td>{String(i.rejected_rows)}</td><td>{new Date(String(i.created_at)+"Z").toLocaleString("en-AU")}</td></tr>)}</tbody></table>:<div className="empty-state">No imports yet.</div>}</section></Shell>}
