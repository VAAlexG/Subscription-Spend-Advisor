"use client";
import {useState} from "react"; import {Shell} from "@/components/shell";
export default function Page(){const [file,setFile]=useState("");return <Shell active="Imports"><div className="page-head"><div><div className="eyebrow">Transaction intake</div><h1>Import Xero CSV</h1><p>Upload, map and validate transactions before recurring-payment analysis.</p></div></div><div className="grid"><section className="card"><div className="card-title">1. Choose a client and file</div><div style={{display:"grid",gap:14,marginTop:20}}><label><div className="metric-label">Client workspace</div><select className="btn" style={{width:"100%"}}><option>Acme Digital</option><option>Harbour Health</option></select></label><label style={{border:"2px dashed var(--line)",borderRadius:10,padding:35,textAlign:"center",cursor:"pointer"}}><input type="file" accept=".csv" hidden onChange={e=>setFile(e.target.files?.[0]?.name||"")}/><b>{file||"Select a Xero CSV export"}</b><div className="muted">Bank transactions · CSV up to 20 MB</div></label><button className="btn primary" disabled={!file}>Continue to column mapping</button></div></section><aside className="card"><div className="card-title">Import safeguards</div><div className="renewal"><div><h4>Duplicate protection</h4><p>Date, amount, account and transaction reference are fingerprinted.</p></div></div><div className="renewal"><div><h4>Source retained</h4><p>Original rows remain linked for traceability and re-analysis.</p></div></div><div className="renewal"><div><h4>Validation first</h4><p>Invalid rows are reported without blocking valid transactions.</p></div></div></aside></div></Shell>}
